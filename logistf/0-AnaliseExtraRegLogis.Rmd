---
title: "0-AnalisesRegLogis"
author: "Jaqueline Lamas da Silva"
date: "2026-05-19"
output:
  html_document: default
  pdf_document: default
format: 
  html:
    self-contained-math: true
    embed-resources: true
    code-fold: true
toc: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
source("C:/ufjf/2025.1/laboratorio_de_praticas_estatisticas/dados.R")
library("logistf")
library("kableExtra")
suppressMessages(library("dplyr"))

suppressMessages(attach(analise2.new))
```

## Verduras e Legumes

```{r}
y<-adeq_porcoes_dia_verduras_legumes=="I" # inadequação
table(y)
```
19 eventos do desfecho.

```{r, warning=FALSE}
t(summary(analise2.new[,c(2:13,15)]))

covariaveis.clinicas<-c("idade_1a_crise_meses", "tea", "di", "tdah", 
  "dificuldade_motora", "disfagia", "rec_vomito_diarreia", "constipacao", 
  "atraso_desenvolvimento_sn", "epilepsia_farmacorressistente", 
  "tipo_focal_generalizada", "etiologia")

covariaveis.clinicas.frequentes<-c("idade_1a_crise_meses", "tea", "di",
  "dificuldade_motora", "constipacao", 
  "atraso_desenvolvimento_sn", "epilepsia_farmacorressistente", 
  "tipo_focal_generalizada", "etiologia")


tabela.pvalor<-function(covariaveis.clinicas.frequentes)
{

    p.valores<-numeric(length(covariaveis.clinicas.frequentes))
    for(i in seq_along(covariaveis.clinicas.frequentes))
    {
      modelo<-logistf(y ~ idade_atual_anos + sexo + get(covariaveis.clinicas.frequentes[i]))
      saida<-capture.output(resumo<-summary(modelo))
      p.valores[i]<-resumo$prob[4]
      limites_log <- confint(modelo)

      # 2. Extrai os coeficientes pontuais (betas)
      coef_log <- modelo$coefficients
      
      # 3. Aplica a função exponencial (exp) para transformar tudo em Odds Ratio
      OR_pontual <- exp(coef_log)       # Odds Ratio pontual da covariável i
      LI_firth   <- exp(limites_log[, 1]) # Limite Inferior
      LS_firth   <- exp(limites_log[, 2]) # Limite Superior
      

      tabela2<-data.frame(OR_pontual,LI_firth,LS_firth)
      colnames(tabela2)<-c("OR","LI", "LS")
      cat("\n\nTabela ODDS ", covariaveis.clinicas.frequentes[i],"\n")
      print(tabela2)
      cat("\n Tamanho do intervalo (LS/LI): ",LS_firth[4]/LI_firth[4],"\n")
      cat("\n")
    }
    print(table(y))
    
    linhas_para_negratar <- which(sort(p.valores)< 0.20)
    tabela<-data_frame(covariaveis.clinicas.frequentes,p.valores)
    
    tabela.final <-tabela |>
      arrange(p.valores) |>
      kableExtra::kable() |>
      row_spec(linhas_para_negratar, bold = TRUE, background = "#f0f8ff") 
    
    significativas<-covariaveis.clinicas.frequentes[which(p.valores< 0.20)]
    for(i in seq_along(significativas))
    {
      cat("\n", significativas[i],"\n")
      print(table(y,get(significativas[i])))
    }
    return(tabela.final)

}
tabela.final<-tabela.pvalor(covariaveis.clinicas)
```


```{r, results='asis'}
print(tabela.final)
```

## Modelo selecionado
As covariáveis **constipacao, tea, dificuldade_motora e di** são significativas a um nível de 20%. 
```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + tea +constipacao+ dificuldade_motora + di)


ajuste<-function(modelo)
{
  limites_log <- confint(modelo)

  # 2. Extrai os coeficientes pontuais (betas)
  coef_log <- modelo$coefficients
  # 3. Aplica a função exponencial (exp) para transformar tudo em Odds Ratio
  OR_pontual <- round(exp(coef_log),4)       # Odds Ratio pontual da covariável i
  LI_firth   <- round(exp(limites_log[, 1]),4)# Limite Inferior
  LS_firth   <- round(exp(limites_log[, 2]),4)# Limite Superior
  tabela2<-data.frame(OR_pontual,LI_firth,LS_firth, round(LS_firth/LI_firth,4), round(modelo$prob,4)
  )
  colnames(tabela2)<-c("OR","LI", "LS","(LS/LI)", "P-valor")
  cat("\n\nTabela ODDS \n")
  print(tabela2)
  cat("\n")
}

table(y, constipacao)
ajuste(modelo)
```

```{r}
table(constipacao,di)
fisher.test(table(constipacao,di))

table(y, di)
table(y, dificuldade_motora)

```

Retirando constipacao.
```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + tea + dificuldade_motora + di)
ajuste(modelo)
```

Retirando dificuldade motora
```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + tea  + di)
ajuste(modelo)
```

Retirando di
```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + tea  + dificuldade_motora)
ajuste(modelo)
```

### Vamos ficar com o modelo *y ~ idade_atual_anos + sexo  + tea +di*
```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + tea + di  )
ajuste(modelo)

summary(modelo)
```
A conclusão sobre o TEA é a mesma, a condição clínica TEA reduz a chance de ter um consumo inadequado de verduras. A conclusão sobre Intellectual developmental disorder também é a mesma. Porém, os coeficientes estão mais estáveis. Olhando pela razão entre o LS e o LI conseguimos ter uma noção melhor sobre a amplitude do intervalo, antes a amplitude do intervalo para a covariável di era `r 544.15/1.65 ` e para a variável tea era `r 0.66/0.008`. As covariáveis constipation e di parecem apresentar uma associação e como a constipação só apresenta um caso em uma das células achei que fizesse mais sentido manter di. 


## Frutas

```{r}
adeq_porcoes_dia_frutas<-analise2.new$adeq_porcoes_dia_frutas
y=adeq_porcoes_dia_frutas=="I" # inadequação
table(y)
```

```{r}
tabela.final<-tabela.pvalor(covariaveis.clinicas)
```


```{r, results='asis'}
print(tabela.final)
```


Fazer o ajuste com **di e atraso_desenvolvimento**. A variável rec_vomito_diarreia possui poucos casos, o IC é muito grande.

```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + di + atraso_desenvolvimento_sn )
ajuste(modelo)

summary(modelo)
```
```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + atraso_desenvolvimento_sn)
ajuste(modelo)

summary(modelo)
```
```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + di )
ajuste(modelo)

summary(modelo)
```


## Laticinios


```{r}
adeq_porcoes_dia_laticinios
y=adeq_porcoes_dia_laticinios=="I" # inadequação
table(y)
```

```{r}
tabela.final<-tabela.pvalor(covariaveis.clinicas)
```

```{r, results='asis'}
print(tabela.final)
```


## Sugary Snacks

```{r}
adeq_porcoes_dia_doces_salgadinhos_guloseimas
y=adeq_porcoes_dia_doces_salgadinhos_guloseimas=="I" # inadequação
table(y)
```

```{r}
tabela.final<-tabela.pvalor(covariaveis.clinicas)
```

```{r, results='asis'}
print(tabela.final)
```


```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + tipo_focal_generalizada + etiologia +constipacao)
ajuste(modelo)

summary(modelo)
```
```{r}
table(y, tipo_focal_generalizada)
```
```{r}
table(y, etiologia)
```
```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + tipo_focal_generalizada + etiologia)
ajuste(modelo)

summary(modelo)
```
```{r}
modelo<-logistf(y ~ idade_atual_anos + sexo  + tipo_focal_generalizada)
ajuste(modelo)
```



